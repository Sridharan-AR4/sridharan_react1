import { Router, createBrowserRouter } from "react-router-dom";
createBrowserRouter([
    {path:"/",element:<Router/>},
    {path:"/about",element:<About/>}
])

const Router =()=>{
    return(
        

    );
};